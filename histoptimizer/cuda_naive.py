# Block Grid is a three-dimensional array where first dimension is the first divider location, second is second,
# and the third is the third location.
#
# Or maybe those are the dimensions of the "thread grid" and the block grid is thread dimension / block size I guess.

